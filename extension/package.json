{
  "name": "think-social-extension",
  "version": "1.0.0",
  "description": "Think Social Chrome Extension",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "typescript": "^5.3.3"
  }
}
